#include<stdio.h>
#include<math.h>

int main() {

  int a=4, b=2, sq, power;

  power = pow(4,2);
  sq = sqrt(4);

  printf("Square of %d is %d", a, power);
  printf("\nSquare root of %d is %d", a, sq);

  return 0;
}